"""
consts.py
=========
כל הקבועים של המשחק במקום אחד. אין "מספרי קסם" בשאר המודולים -
כל מספר שמופיע בקוד אחר מחושב מתוך הקבועים כאן.
שינוי ערך אחד כאן משנה את כל המשחק בהתאם.
"""

# ---------------------------------------------------------------------------
# לוח המשחק
# ---------------------------------------------------------------------------
BOARD_ROWS = 25            # מספר שורות במטריצה
BOARD_COLS = 50            # מספר עמודות במטריצה
CELL_SIZE = 20             # פיקסלים לכל משבצת

# גודל החלון נגזר מגודל הלוח, ולא להפך
WINDOW_WIDTH = BOARD_COLS * CELL_SIZE
WINDOW_HEIGHT = BOARD_ROWS * CELL_SIZE

WINDOW_TITLE = "The Flag"

# ---------------------------------------------------------------------------
# סימוני משבצת במטריצה
# ---------------------------------------------------------------------------
EMPTY = "EMPTY"           # משבצת ריקה
MINE = "MINE"             # משבצת שיש בה מוקש
FLAG = "FLAG"             # משבצת ששייכת לדגל

# ---------------------------------------------------------------------------
# החייל
#   הדמות תופסת SOLDIER_COLS על SOLDIER_ROWS משבצות.
#   השורות העליונות הן ה"גוף", השורה התחתונה היא ה"רגליים".
# ---------------------------------------------------------------------------
SOLDIER_ROWS = 4
SOLDIER_COLS = 2
SOLDIER_BODY_ROWS = 3     # החלק העליון - נבדק מול הדגל
SOLDIER_FEET_ROWS = 1     # החלק התחתון - נבדק מול המוקשים

SOLDIER_START_ROW = 0     # פינה שמאלית-עליונה בתחילת המשחק
SOLDIER_START_COL = 0

# ---------------------------------------------------------------------------
# הדגל - תופס FLAG_COLS על FLAG_ROWS משבצות, בפינה הימנית-תחתונה
# ---------------------------------------------------------------------------
FLAG_ROWS = 3
FLAG_COLS = 4

# ---------------------------------------------------------------------------
# מוקשים - כל מוקש תופס MINE_COLS משבצות ברוחב ו-MINE_ROWS בגובה
# ---------------------------------------------------------------------------
MINES_COUNT = 20
MINE_ROWS = 1
MINE_COLS = 3
MINE_GAP = 1              # רווח מינימלי (במשבצות) בין מוקש למוקש

# ---------------------------------------------------------------------------
# שיחים - קישוט בלבד, מפוזרים אקראית על הרקע הירוק
# ---------------------------------------------------------------------------
BUSHES_COUNT = 20
BUSH_MIN_CELLS = 2         # גודל שיח מינימלי במשבצות
BUSH_MAX_CELLS = 3         # גודל שיח מקסימלי במשבצות
BUSH_PLACE_ATTEMPTS = 300  # כמה פעמים לנסות למקם שיח בלי חפיפה

# ---------------------------------------------------------------------------
# זמנים
# ---------------------------------------------------------------------------
FPS = 60
REVEAL_MS = 1000         # משך חשיפת המוקשים בלחיצה על Enter (שנייה אחת)
END_MESSAGE_MS = 3000    # משך הודעת הניצחון / ההפסד לפני שהחלון נסגר

# ---------------------------------------------------------------------------
# טקסטים
# ---------------------------------------------------------------------------
WELCOME_MESSAGE = "Welcome to The Flag game.\nHave Fun!"
WIN_MESSAGE = "You reached the flag!"
LOSE_MESSAGE = "You stepped on a mine!"

FONT_NAME = "Arial"
WELCOME_FONT_SIZE = 22
MESSAGE_FONT_SIZE = 40

# ---------------------------------------------------------------------------
# צבעים (R, G, B)
# ---------------------------------------------------------------------------
FIELD_COLOR = (46, 139, 46)       # רקע ירוק - "שדה" במצב רגיל
REVEAL_BG_COLOR = (15, 28, 15)    # רקע כהה במצב חשיפת המוקשים
GRID_LINE_COLOR = (55, 105, 55)   # קווי רשת המשבצות במצב החשיפה
WELCOME_TEXT_COLOR = (255, 255, 255)
WIN_TEXT_COLOR = (235, 255, 235)
LOSE_TEXT_COLOR = (255, 120, 120)
MESSAGE_BOX_COLOR = (0, 0, 0)     # רקע מלבן ההודעה במרכז המסך

# ---------------------------------------------------------------------------
# קבצי תמונה (חייבים לשבת באותה תיקייה כמו הקוד)
# ---------------------------------------------------------------------------
SOLDIER_IMAGE = "soldier.png"
FLAG_IMAGE = "flag.png"
MINE_IMAGE = "mine.png"
BUSH_IMAGE = "flower.png"

# ---------------------------------------------------------------------------
# מצבי המשחק - קובעים מה קורה בכל פריים
# ---------------------------------------------------------------------------
STATE_PLAY = "play"       # משחק רגיל, החייל נע, המוקשים חבויים
STATE_REVEAL = "reveal"   # המוקשים גלויים לשנייה, החייל קפוא
STATE_WIN = "win"         # הודעת ניצחון לפני סגירה
STATE_LOSE = "lose"       # הודעת הפסד לפני סגירה

# ---------------------------------------------------------------------------
# גדלים נגזרים בפיקסלים - כל תמונה מוקטנת בדיוק לשטח המשבצות שלה,
# כדי שמה שהשחקן רואה יתאים בדיוק למה שנבדק במטריצה.
# ---------------------------------------------------------------------------
SOLDIER_PIXEL_SIZE = (SOLDIER_COLS * CELL_SIZE, SOLDIER_ROWS * CELL_SIZE)  # 40 x 80
FLAG_PIXEL_SIZE = (FLAG_COLS * CELL_SIZE, FLAG_ROWS * CELL_SIZE)           # 80 x 60
MINE_PIXEL_SIZE = (MINE_COLS * CELL_SIZE, MINE_ROWS * CELL_SIZE)           # 60 x 20
